# AGENTS.md · AI 协作约束与边界

> 本文写给在本仓库工作的 AI（Claude Code 等）。人类进度看 [WORK.md](WORK.md)。

## 项目一句话

TickCap：时刻记录 + 竖向时间轴 + AI 复盘的多端记录工具。Web H5 优先，小程序次之，App 预留。

## 动手前先读什么（真理来源与裁决顺序）

| 要做的事 | 权威文档 |
|----------|----------|
| 实现任何功能 | [docs/07-详细技术设计](docs/07-详细技术设计.md)（领域规则/DDL/API 契约/验收标准）——**与 05 冲突时以 07 为准** |
| 写任何 UI | [docs/06-UI设计规范](docs/06-UI设计规范.md)（令牌/材质系统/界面图纸/文案语气） |
| 碰 AI 生成 | [docs/08-AI复盘技术设计](docs/08-AI复盘技术设计.md) |
| 加埋点 | [docs/09-埋点与数据指标](docs/09-埋点与数据指标.md)（事件字典是闭集，新事件先补字典） |
| 判断做不做某功能 | [docs/04-路线图](docs/04-路线图.md)（MVP 砍单清单）+ [docs/02-功能设计](docs/02-功能设计.md)（P0/P1/P2） |

术语统一用 07 §1 术语表（胶囊/滴答/封存/归属日/日界/空隙/Streak），代码命名与之对应（Capsule/tick/seal/date/dayBoundary/gap/streak）。

## 架构边界（违反即返工）

1. **领域逻辑只住 `packages/core`**：归属日、时间推断、streak、空隙、统计等规则一律实现在 `@tickcap/core`（纯 TS，禁止 import React/Node/浏览器 API），UI 和 server 只调用不复制。core 的规则改动**必须带单元测试**（跨日界/宽限期/解封重封等边界用例）。
2. **视觉值只来自 `@tickcap/tokens`**：任何组件不得硬编码颜色/字号/间距/圆角/动效时长的字面量；缺令牌先补令牌。
3. **契约先行**：改 API 先改 docs/07 §4，再改 `@tickcap/api` 的 zod schema，最后改实现；DB 变更只能走 drizzle 迁移文件，禁止手改 schema。
4. **Prompt 即代码**：提示词只存 `apps/server/prompts/` 版本化文件（`daily-v1.md` 式命名），禁止散落在业务代码字符串里；报告必须记录 `prompt_version`。
5. **依赖克制**：新增依赖需在 PR/提交说明里给理由；禁止引入重型 UI 组件库和第三方埋点/统计 SDK。

## 产品红线（代码层强制，无例外）

- **3 秒原则**：常规记录路径 ≤3 次交互；任何往记录主路径上加步骤的改动默认拒绝。
- **隐私硬边界**：`is_private` 胶囊的 summary/detail/mood 永不进入 LLM 请求（打包层物理剔除，不靠 prompt 承诺）；埋点永不上报用户正文内容。
- **AI 是教练不是作者**：AI 输出必须可编辑，`edited_md` 永不被重新生成覆盖；封存流程不依赖 AI 成功（失败走本地降级版）。
- **诚实的镜子**：任何 UI 文案不得评判用户（摸鱼/发呆/断签零负面），文案语气遵循 06 §7 对照表。
- **永不做**：广告、记录条数限制、导出收费、删除挽留弹窗。

## 工程规范

- TypeScript 严格模式（继承 tsconfig.base.json），不用 `any` 逃逸；时间处理统一 UTC 存储 + 用户时区换算，换算逻辑只在 core。
- 提交用 conventional commits（`feat: ` / `fix: ` / `docs: ` 等，中文描述可）；一个提交一个主题。
- 测试底线见 07 §7：core 单测必须全绿才算完成；测试中**禁止调用真实大模型 API**（用 fixture/mock）。
- 密钥与配置走环境变量（`.env` 已 gitignore），任何密钥出现在代码或提交里都是事故。

## 行为边界

- **不擅自推翻 docs/ 中的既定决策**：发现文档有问题，先在 [docs/10-评审记录](docs/10-评审记录.md) 追加记录并说明，经确认后再改动关联文档与代码。
- **不擅自 commit/push**，除非用户明确要求。
- 完成里程碑或做出新决策后，**同步更新 [WORK.md](WORK.md)**（勾选清单/追加决策日志一行），保持它是项目状态的唯一快照。
- 超出当前阶段的功能（见 04 路线图）不实现、不预埋半成品代码；确有必要预留的，只预留 schema/接口形状（已在 07 定义的范围内）。
